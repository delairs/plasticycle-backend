apiVersion: v1
kind: Service
metadata:
  name: plasticycle-backend
  namespace: prod
spec:
  type: NodePort
  selector:
    app: plasticycle-backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30085